spring.application.name=expense-tracker

# Active Profile
# Set to 'local' for H2/JDBC or 'staging' for PostgreSQL/JPA
# You can also set this via environment variable: SPRING_PROFILES_ACTIVE=local
# Or via command line: java -jar app.jar --spring.profiles.active=local
spring.profiles.active=local

